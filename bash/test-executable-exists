---
syntax: bash
tags:
  - conditional
  - exists
  - file
  - if
  - test
---
# test if command `foo` is available on the `$PATH`
if ! [ -x "$(command -v foo)" ]; then
  # the `foo` executable is not on the path
fi
