---
syntax: javascript
tags:
  - array
  - javascript
  - filter
---
// filter `arr` to contain only even numbers
let arr = [1, 2, 3, 4, 5, 6];
let evens = arr.filter(item => {
  return item % 2 == 0;
});
